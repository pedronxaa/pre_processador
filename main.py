from pre_processador import pre_processar
from sys import argv

#caminho_h = input('Digite o caminho da pasta das bibliotecas padrão: ')
#caminho_arquivo = input('Digite o caminho do arquivo em c: ')
print(argv[0])
pre_processar(argv[1],'')